<UserControl x:Class="LedCube.Core.UI.Controls.SettingsDialog.SettingsPage_GeneralConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LedCube.Core.UI.Controls.SettingsDialog"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:GeneralCubeSettingsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock FontSize="16" FontWeight="SemiBold" Text="Cube Dimensions" Margin="0,0,0,12"/>

        <!-- Adjust X/Y/Z property names to match your CubeDimensions type -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="X"/>
            <TextBox   Grid.Row="0" Grid.Column="1"
                       Text="{Binding Dimensions.X, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Y" Margin="0,8,0,0"/>
            <TextBox   Grid.Row="1" Grid.Column="1" Margin="0,8,0,0"
                       Text="{Binding Dimensions.Y, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Z" Margin="0,8,0,0"/>
            <TextBox   Grid.Row="2" Grid.Column="1" Margin="0,8,0,0"
                       Text="{Binding Dimensions.Z, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
    </StackPanel>
</UserControl>
